# Gate 0

Old unresolved obligation: an actual inverse from prescribed n4 hive interior
coordinates to a real symmetric matrix pair. This is stronger than finding
one pair for the boundary spectra.

Map tested: h(p,q)=max Tr(A E+C F) over mutually orthogonal projections of
ranks p,q, where C=A+B. Candidate image constraints are proved by equality
cases of variational eigenvalue bounds, not invented matrix parameterizations.

GO for a substantive exclusion requires a valid exact hive and exclusion of
every Hermitian realization. If only a supplied-frame example is found,
FRONTIER_DELTA=NONE. No numerical miss or dimension count meets this gate.

Complexity: same matrix/hive spaces and quantifiers; no hidden inverse moved
to another selector. An exclusion would remove this specific universal map
premise, not solve or disprove general inverse Horn or all possible hive maps.
