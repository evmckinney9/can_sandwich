# R391: prove or kill the supplied T4 normal form

Explicit user task: test T4, which requires A real symmetric tridiagonal
while C=A+B is diagonal up to any permutation of its prescribed spectrum.

Hypothesis R0391-H1: every feasible traceless real n4 additive Horn triple
has such a representation. Acceptance: universal proof or exact feasible
counterexample excluding every permutation. Cheapest falsifier: a shifted
rank-one A has support at most two in a tridiagonal basis, forcing retained
diagonal eigenvalues of B that strict rank-one interlacing forbids.

Candidate derived on paper before this written gate: A=J-I and
C=diag(3,1,-1,-3). This ordering of discovery is disclosed, not represented
as prospective preregistration. Freeze the candidate before exact checks.
Research integrity passed. Budget: one candidate, one symbolic certificate,
one independent review; <=1GiB/30sCPU/45swall, no production/corpus.

Forward obligation: certify an actual feasible spectral triple.
Inverse/coverage obligation: exclude all tridiagonal witnesses for its
spectra, not only the displayed pair or one eigenvalue ordering.
FRONTIER_DELTA = REFUTE for T4; no global inverse-Horn solution follows.
The existing simultaneous unitary tridiagonalization theorem is a different
claim: it neither diagonalizes C nor guarantees a real orthogonal change.
No successor normal form is to be explored after this falsification.
