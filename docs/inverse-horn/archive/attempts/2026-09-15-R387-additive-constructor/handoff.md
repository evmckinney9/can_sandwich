# R0387 handoff

Terminal scope: a spectra-only additive n=4 numerical witness constructor has
been implemented and exercised on one frozen 12-case diagnostic packet.
Full acceptance failed: five UNKNOWN cases remain with the block wrapper.
Plain scaling returns5 witnesses; the wrapper returns7, gaining the paired
repeated-spectrum and exact2+2-block cases. There were no post-packet solver
changes. This is finite implementation evidence, not a universal constructor
proof, convergence bound, or hive parameterization.

First remaining obligation: practical completion to the prescribed tolerance
on the five missed inputs, together with a justified total initialization and
precision policy for any broader claim. The source convergence theorem is not
refuted by a6000-sweep cap. The3x3 child on the1+3 case had a1000-sweep cap.
The supplied-hive inverse remains a separate, unchanged open obligation.

Evidence: constructor.py, inputs.json, separately stored plants.json,
results.json, verifier.py, independent.json, review.md and README.md.
The solver reads only the three spectral lists and its own fixed seed.
Reproduction and command limits appear in README.md; each computational
command used one BLAS thread and1GiB/30sCPU/45swall ceilings.

Complexity change: numerical realization is now executable; no mathematical
complexity reduction is established. The block wrapper has explicit finite
candidate and child budgets. Fixed-seed coverage, accuracy certification and
all-strata practical completion remain unproved. FRONTIER_DELTA=NONE for the
open mathematical problem.

CLAIMS.yaml records R0387-M1 as observed. Diagnostic input/output files are
hashed in evidence/corpora.toml. An existing uninvolved reviewer context was
reused under AGENTS.md for independent matrix verification and source-scope
review. No user approval is required for this local research record.
