# Gate 0

Old obligation: construct a real additive n=4 witness from feasible spectra.
Stronger proposed hive parameterization remains unsolved and is not inferred.

Operation: shift/scale -> common Cholesky and separate QR -> relative real
eigenframe. For trace-compatible spectral partitions, solve smaller blocks
and assemble a candidate, checking the original full target before accepting.
The partition enumeration supplies no general theorem from trace equality
alone; unsuccessful candidates are discarded.

GO means checked matrices on the declared finite packet, with failures and
costs reported. A failed packet ends tuning; it is not a refutation of scaling.

FRONTIER_DELTA = NONE for new mathematical theory or hive coverage. The
deliverable is a reproducible numerical constructor and a measured boundary
comparison, not a source-only proposal or a claimed new general algorithm.

Complexity: three n-by-n frames replace a single SO(n) frame internally;
positive affine normalization; up to6000 QR/Cholesky sweeps; at most8 candidate
block partitions with child limits1000. Search, finite-seed coverage, numerical
conditioning and certification remain charged. No radical degree claim.
